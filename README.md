# Medical Survey Parser Bot

Telegram bot for parsing medical clinic survey data and saving to Yandex Spreadsheet.

## Setup

1. Create bot via @BotFather
2. Set environment variables:
   - `BOT_TOKEN`
   - `YANDEX_OAUTH_TOKEN` 
   - `SPREADSHEET_ID`
3. Deploy to Render
